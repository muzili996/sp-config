spring:
  application:
    name: item-service
  cloud:
    config:
      override-none: true

server:
  port: 8001

eureka:
  client:
    service-url:
      #defaultZone   eureka 服务器地址
      defaultZone: http://eureka1:2001/eureka,http://eureka2:2002/eureka

